name: Passo 17 - Windows Portable

on:
  workflow_dispatch:
  push:
    branches:
      - migration/python-foundation
    paths:
      - 'MonitorDeNoticias.spec'
      - 'requirements-build.txt'
      - 'scripts/build_portable.ps1'
      - 'scripts/validate_portable.ps1'
      - 'scripts/pyi_runtime_portable_validation.py'
      - 'tools/build-globoplay-login-helper.py'
      - 'tools/globoplay-login-helper.py'
      - 'portable/**'
      - '.github/workflows/pass17-build-portable.yml'

permissions:
  contents: read

jobs:
  build:
    name: build-windows-x64
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - name: Checkout branch real
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Confirm clean worktree and frozen commit
        shell: pwsh
        run: |
          $status = git status --porcelain
          if ($status) { throw "Worktree não está limpa antes da build: $status" }
          $sha = (git rev-parse HEAD).Trim()
          if ($sha -ne '${{ github.sha }}') { throw "HEAD $sha difere do github.sha ${{ github.sha }}" }
          Write-Host "WORKTREE_CLEAN=YES"
          Write-Host "FROZEN_COMMIT=$sha"

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install runtime test dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Regression suite before packaging
        shell: pwsh
        env:
          QT_QPA_PLATFORM: offscreen
        run: |
          python -m compileall -q src tests scripts run.py
          python -m pytest -ra --durations=20

      - name: Build clean portable
        shell: pwsh
        run: |
          ./scripts/build_portable.ps1

      - name: Capture build inventory
        shell: pwsh
        run: |
          $root='dist/MonitorDeNoticias'
          Get-Content "$root/BUILD-INFO.json"
          Get-ChildItem "$root/_internal" -Recurse -Filter 'qwindows.dll' | Select-Object FullName,Length
          Get-ChildItem "$root/_internal" -Recurse -Filter 'Qt6Multimedia*.dll' | Select-Object FullName,Length
          Get-ChildItem "$root/_internal" -Recurse -File | Where-Object {$_.FullName -match 'plugins\\multimedia'} | Select-Object FullName,Length
          Get-ChildItem 'portable-out' | Select-Object Name,Length

      - name: Upload portable candidate
        uses: actions/upload-artifact@v4
        with:
          name: MONITOR-DE-NOTICIAS-PYTHON-portable-windows-x64
          if-no-files-found: error
          compression-level: 0
          retention-days: 14
          path: |
            portable-out/MONITOR-DE-NOTICIAS-PYTHON-portable-windows-x64.zip
            portable-out/MONITOR-DE-NOTICIAS-PYTHON-portable-windows-x64.zip.sha256
            scripts/validate_portable.ps1

  validate-clean:
    name: validate-zip-without-repository
    needs: build
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - name: Download only portable artifact and external validator
        uses: actions/download-artifact@v4
        with:
          name: MONITOR-DE-NOTICIAS-PYTHON-portable-windows-x64
          path: artifact

      - name: Prove repository/source are absent
        shell: pwsh
        run: |
          if(Test-Path 'src'){throw 'src inesperado no ambiente de validação.'}
          if(Test-Path 'tests'){throw 'tests inesperado no ambiente de validação.'}
          if(Test-Path '.git'){throw '.git inesperado no ambiente de validação.'}
          Get-ChildItem artifact -Recurse | Select-Object FullName,Length

      - name: Validate re-extracted ZIP in clean path
        shell: pwsh
        run: |
          $zip=(Get-ChildItem artifact -Recurse -Filter 'MONITOR-DE-NOTICIAS-PYTHON-portable-windows-x64.zip' | Select-Object -First 1).FullName
          $validator=(Get-ChildItem artifact -Recurse -Filter 'validate_portable.ps1' | Select-Object -First 1).FullName
          if(-not $zip -or -not $validator){throw 'Artefato/validador ausente.'}
          & $validator -ZipPath $zip
